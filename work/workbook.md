# Workbook

Tony works here. Sibling files are short facts; this file is the running log. Add dates. Keep entries short.

---

## 2026-09-08

### How we work
- Chat decides product. **jarvis-builder** (Composer 2.5) implements.
- One aspect at a time. Research first, then mail.

### Brain
- Switched from tiny Ollama as the work brain to **Gemini API** (`gemini-2.5-flash`, `LLM_PROVIDER=auto`).
- Ollama stays available; force later with `LLM_PROVIDER=ollama`.
- “Task for Gemini” mail is still a separate tool.

### Research
- Board was a link dump; small Ollama invented prices.
- Now: tool gathers pages; Gemini narrates only from notes.
- Live check: aluminium prices in India — good.

### HUD
- Header **Mic always on** / **Mic off** (saved in the browser).
- Off = no wake word. Space + orb still listen for a command.

### Mail
- Same pattern as research on live Gmail. Builder shipped find/read/reply/compose/forward/attachments.
- Live check: Deepak enquiry RFNW-2600105 — body + parts table + PDF drawings on the board.
- This Gemini key will not take PDF as vision; we extract drawing text instead. Images still go through vision. Canvas later.
- Shall I before send or forward. Do not send unless Yes.
- Attachment save (tick → disk + Drive, attach to reply) is **shipped**. HUD: tick PDF → Save selected. Deepak piston `DPIS000377S3N0000.pdf` saved under `backend/exports/drawings/` and Drive.
- **Viewer shipped:** HUD overlay. Saved local files only. Zoom/pan/pages, crop, markers, save marked PNG to `exports/drawings` + Drive. Voice close/zoom/next stays on the HUD. PDF render needs the file already saved. See `work/viewer.md`.

### Briefing
- Shipped: “brief me” / “catch me up”. Speak = who needs you + next on the clock. Board = **Who needs you** (From + Subject) only.
- Banks / LinkedIn / Amazon filtered. Live check excluded those; inbox had no Deepak/Neha unread — board showed **Ant** (Supabase). No calendar events that hour, so no “next up”.
- Glance still waits.

### Still to redo
Calendar, files/Drive, Task-for-Gemini, ordinary chat, canvas as a separate workspace. Attach marked drawing to a reply can follow the viewer.

---

## Scratch

(Tony: notes, phrasing to try, names from the live inbox.)
